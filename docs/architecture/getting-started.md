# Aesir Architecture — 快速开始

!!! note "占位"

    本节内容整理中。安装方式(Git URL / unitypackage)与第一个 MVC 示例将在此补齐。

## 预期内容

1. 安装 Aesir Architecture(Git URL 或 Release unitypackage)
2. 创建第一个 Context 并注册 Model / Service
3. 跑通 MVC 快捷档计数器示例