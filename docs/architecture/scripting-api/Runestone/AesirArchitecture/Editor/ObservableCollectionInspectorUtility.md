---
title: ObservableCollectionInspectorUtility
description: "Runestone.AesirArchitecture.Editor.ObservableCollectionInspectorUtility 的 API 文档"
---

# `ObservableCollectionInspectorUtility`

!!! note ""

    - **种类:** `static class`
    - **命名空间:** `Runestone.AesirArchitecture.Editor`
    - **程序集:** `Runestone.AesirArchitecture.Editor.OdinInspector`

**继承链:** `System.Object` → `ObservableCollectionInspectorUtility`

## 声明

``` csharp
internal static class ObservableCollectionInspectorUtility
```

可观察集合调试面板的反射工具 —— 在编辑器侧读取集合的运行时状态。

**备注**

面板是调试工具而非热路径，这里统一用反射读取（无需运行时程序集暴露调试接口）。

## 方法

<div class="api-summary-table" markdown="1">

| 名称 | 描述 | 声明类型 |
| :--- | :--- | :--- |
| `GetType()` | — | `object` |
| `Equals(object)` | — | `object` |
| `GetHashCode()` | — | `object` |
| `ToString()` | — | `object` |
| `MemberwiseClone()` | — | `object` |
| `Finalize()` | — | `object` |

</div>

## Additional Notes

> 首个 `## Additional Notes` 是增量生成文档标识符，请勿修改标题级别和内容！本文档由 [`Script Doc Generator`](https://github.com/yuumixcode/AesirFramework) 辅助生成。
