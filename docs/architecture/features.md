# Aesir Architecture — 特性

!!! note "占位"

    本节内容整理中。

## 预期内容

- 能力接口组合与角色矩阵(Controller / Presenter / Command / Query / Service / Model)
- 上下文系统(`IContext` / `AbstractContext<T>`)
- `ObservableValue<T>` / `ObservableList<T>` 等响应式数据
- `MiniEvent` 轻量事件与 `MonoLifecycleProxy` 生命周期代理
- 六档渐进式示例家族(MVC×3 + MVP×3)