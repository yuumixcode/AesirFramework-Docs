# Aesir Modules — 特性

!!! note "占位"

    本节内容整理中。

## 预期内容

- `UIModule`:静态 API(`Show<T>` / `Hide<T>` / `Prewarm<T>`)与面板生命周期
- `UIRoot`:四层 Canvas 层级 + `UILayer` 枚举
- `IUIAssetLoader` / `ResourcesUILoader`:可插拔资源加载
- EventModule V2:属性注册 + 动态注册双机制