---
title: BinderMenuItems
description: "Runestone.AesirModules.BinderMenuItems 的 API 文档"
---

# `BinderMenuItems`

!!! note ""

    - **种类:** `static class`
    - **命名空间:** `Runestone.AesirModules`
    - **程序集:** `Runestone.AesirModules.OdinInspector`

**继承链:** `System.Object` → `BinderMenuItems`

## 声明

``` csharp
internal static class BinderMenuItems
```

Binder 层级右键菜单快捷入口: 为选中物体快速挂载 BinderAssistant 与 BinderTag， 免去 Add Component 菜单的层层查找。

## 方法

<div class="api-summary-table" markdown="1">

| 名称 | 描述 | 声明类型 |
| :--- | :--- | :--- |
| `GetType()` | — | `object` |
| `Equals(object)` | — | `object` |
| `GetHashCode()` | — | `object` |
| `ToString()` | — | `object` |
| `MemberwiseClone()` | — | `object` |
| `Finalize()` | — | `object` |

</div>

## Additional Notes

> 首个 `## Additional Notes` 是增量生成文档标识符，请勿修改标题级别和内容！本文档由 [`Script Doc Generator`](https://github.com/yuumixcode/AesirFramework) 辅助生成。
